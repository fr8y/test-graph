{
  "name": "graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ apes/graph",
    "create-local": "graph create --node http://localhost:8020/ apes/graph",
    "remove-local": "graph remove --node http://localhost:8020/ apes/graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 apes/graph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.3",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
